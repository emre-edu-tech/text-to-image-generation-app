# Per-session prompt template

Read `docs/specs/<NN>-<name>.md` and implement exactly what it specifies — nothing more.

Rules:
- Follow the folder structure, schema, and route map from `docs/specs/00-overview.md` exactly (already in your context).
- Do not add features, routes, or files beyond what this step asks for.
- If files from earlier steps already exist, read them first and match the existing code style.
- When you believe you're done, check your work against every item under "Acceptance criteria" at the bottom of the spec file. Fix anything that doesn't pass before finishing.
- If anything in the spec is ambiguous, or you'd have to make a significant judgment call, stop and ask me instead of guessing.

# Worked Example — Step 3
Read `docs/specs/03-public-frontend.md` and implement exactly what it specifies — nothing more.

Rules:
- Follow the folder structure, schema, and route map from `docs/specs/00-overview.md` exactly.
- Do not add features, routes, or files beyond what this step asks for.
- Files from Steps 1–2 (app factory, models, seed data) already exist — read them first and match the existing code style.
- When done, check your work against every item under "Acceptance criteria" in `docs/specs/03-public-frontend.md`.
- If anything is ambiguous, stop and ask instead of guessing.

# If a session runs out of budget mid-step
Continuing `docs/specs/<NN>-<name>.md` — some of this may already be partially implemented.
Check what already exists first, then finish whatever's left, and verify the result
against the "Acceptance criteria" section before finishing.